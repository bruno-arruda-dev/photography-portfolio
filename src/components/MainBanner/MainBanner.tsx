import styles from './MainBanner.module.scss';

const MainBanner = () => {

    return (
        <section className={styles.mainBanner}>
            <h1>O seu fotógrafo profissional</h1>
            <p>Registre seus melhores momentos</p>
        </section>
    );
};

export default MainBanner;